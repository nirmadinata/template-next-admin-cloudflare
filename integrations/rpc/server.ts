/**
 * Server-side RPC exports
 *
 * Use this for server components, server actions, and API routes.
 *
 * @example
 * ```ts
 * import { serverRpc } from "@/integrations/rpc/server";
 *
 * const data = await serverRpc.home.getHomePageData();
 * ```
 */

export { serverRpc } from "./server-client";
